# 茅台物流单模板提取说明

## 来源与完整性

- 来源文件：`D:\备份\MOUTAI物流单\茅台物流单.docx`
- 已核验 SHA-256：`fff3188e073183db78bb70859718900a1662036e1ca29d89742f9009992ee5aa`
- 项目版本化副本：`server/api/assets/templates/moutai-logistics-sheet.docx`
- 原附件保持只读，不在开发或运行时修改。

## 页面结构证据

- 单页 A4 纵向：`w:pgSz w:w="11906" w:h="16838"`。
- 页边距：上/下 `1440` twips，左/右 `1800` twips。
- 正文无表格、图片、域或内容控件。
- 正文为普通段落，字段标签依次为商品名称、香型、酒精含量、规格、数量、配料、
  出厂日期、生产批次、批次序号、物流码、注意事项、收货人、收货日期。
- 原模板正文字号为 `w:sz=36`（18pt），签字区使用右对齐与制表符。
- `w:sectPr` 只在 `w:body` 末尾出现一次。

## 保留与替换规则

- 只替换“商品名称”“出厂日期”“生产批次”“批次序号”“物流码”五个标签后的正文。
- 香型、酒精含量、规格、数量、配料、注意事项和签字区逐字保留。
- 多瓶导出时克隆完整正文页面块，页面之间插入显式分页符，文档末尾只保留一个
  原始 `w:sectPr`。
- 不改字体、18pt 字号、缩进、制表符、页边距、主题和签字区对齐。
- 长商品名称允许按 Word 默认规则自然换行，不缩小字号。
- 所有用户文本先执行 XML 五字符转义，再写入指定 `w:t` 文本槽位。

## 验证计划

- 检查生成文件可作为 ZIP 打开，且 `word/document.xml` 可被 XML 解析。
- 检查一瓶/两瓶页数语义、分页符数量及唯一末尾 `sectPr`。
- 检查字段顺序、前导零、中文长名称与 XML 特殊字符。
- 检查输出不包含进货价、库存 ID 和内部状态。
- 当前环境无 LibreOffice、Microsoft Word 或 WPS，已完成 OOXML 结构验证；仍需在具备
  Word/LibreOffice 的人工验收环境打开样例并逐页检查。
