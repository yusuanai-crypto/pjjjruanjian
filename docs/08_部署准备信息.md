# 部署准备信息

信息来源：阿里云 ECS 控制台截图，保存时间：2026-06-21。  
截图附件：`docs/aliyun/ecs-instance-launch-advisor-20260619.png`

说明：本文只记录部署需要的非敏感信息。服务器密码、SSH 私钥、数据库密码、AccessKey、验证码等敏感信息不要写入仓库。

## 云服务器

- 云厂商：阿里云
- 产品：云服务器 ECS
- 实例名称：launch-advisor-20260619
- 实例 ID：i-f8z85o7o05ynwqzkq2hs
- 运行状态：运行中
- 地域：华南 2（河源）
- 可用区：B
- 公网 IP：47.120.24.23
- 私网 IP：172.30.77.108
- 系统版本：Ubuntu 24.04 64 位
- 实例规格：ecs.u1-c1m2.2xlarge
- CPU：8 核 vCPU
- 内存：16 GiB
- 系统盘：ESSD Entry 云盘，40 GiB
- 系统盘 ID：d-f8zedqzpp9r2xsoga9iz
- 公网带宽：5 Mbps
- 带宽计费方式：按固定带宽
- 付费类型：包年包月
- 创建时间：2026 年 6 月 19 日 20:20:00
- 到期时间：2027 年 6 月 19 日 23:59:59
- 健康状态：正常
- SSH 用户名：root
- SSH 登录方式：已验证公网 SSH 密钥登录可用；阿里云 Workbench 免密连接可作为备用入口
- SSH 密钥名称或存放位置：C:\Users\Msi\.ssh\id_ed25519
- 是否已安装 Docker：否
- 是否已安装 Docker Compose：否

## 域名和 HTTPS

- API 域名：`api.gzjiangjiuguan.com`
- 域名是否已备案：待确认
- DNS 是否已解析到服务器 IP：是，已解析到 `47.120.24.23`
- HTTPS 证书方式：Let's Encrypt + Certbot（Nginx 自动部署，证书到期日 `2026-10-20`，自动续期已启用且续期演练通过）

## 端口和安全组

- 安全组 ID：`sg-f8zj6gbzxrffphra6dh8`
- 安全组类型：普通安全组
- 入方向已确认规则 1：`443/TCP`，协议类型 `HTTPS(443)`，来源 `0.0.0.0/0`，描述 `HTTPS public access`
- 入方向已确认规则 2：`80/TCP`，协议类型 `HTTP(80)`，来源 `0.0.0.0/0`，描述 `HTTP public access`
- 入方向已确认规则 3：`22/TCP`，协议类型 `SSH(22)`，来源 `38.113.126.217/32`，描述 `SSH only my current IP`
- 入方向已确认规则 4：`22/TCP`，协议类型 `SSH(22)`，来源 `100.104.0.0/16`
- `3000/TCP`：仓库当前后端默认使用 `PORT=3000`，但当前安全组入方向未见放行规则，符合“仅供 Nginx 反向代理转发、不直接暴露公网”的预期
- `3306/TCP`：按当前技术方案用于 MySQL，当前安全组入方向未见放行规则，符合“不直接暴露公网”的预期
- 安全组出方向：已查看 `出方向` 标签页，当前规则列表为空，页面显示“暂无数据”

## 数据库

- 数据库类型：阿里云 RDS for MySQL 8.0
- 部署方式：阿里云 RDS 独立实例部署，不与应用共用 ECS
- RDS 实例 ID：`rm-f8zycvw59up98jh62`
- RDS 运行状态：运行中
- 地域和可用区：`华南 2（河源）`，可用区 `B`
- 网络类型：专有网络 VPC
- VPC 网段：`172.16.0.0/12`
- 存储类型：高性能云盘
- 实例规格：`mysql.n2.large.1`
- CPU：4 核
- 数据库内存：8192 MiB
- 默认连接数：6000
- 最大 IOPS：20000
- 存储空间：100 GiB，当前已使用约 3.47 GiB
- 可维护时间段：`02:00-06:00`，时区 `Asia/Shanghai (UTC+8)`
- 数据库名称：当前截图未展示，待确认
- 数据库用户：当前截图未展示，待确认
- 内网连接地址：`rm-f8zycvw59up98jh62.mysql.rds.aliyuncs.com`
- 内网连接端口：`3306`
- 外网连接地址：当前未开通
- 数据库密码存放位置：不要写明文，只写“见密码管理器”

## 备份

- 备份方式：阿里云云盘快照
- 备份时间：当前截图显示最近一批云盘快照创建时间为 `2026 年 6 月 21 日 03:02:00`
- 备份数量：当前地域快照共 `2` 个，总占用约 `6.71 GiB`
- 备份覆盖：系统盘 `40 GiB` 1 个快照，数据盘 `80 GiB` 1 个快照
- 快照来源：`自动创建`
- 保留天数：当前截图显示为 `30 天`
- 备份位置：阿里云 `华南 2（河源）` 地域云盘快照；当前页面显示全部地域快照为 `1 个地域 / 2 个快照`
- 是否需要异地备份：当前截图未见已开启跨地域复制；控制台提示如需更高可用性，建议开启快照跨地域复制

## 苹果开发者账号

- 是否已有苹果开发者账号：待确认
- 是否已开通 TestFlight：待确认
- 账号负责人：待确认
- 账号密码存放位置：不要写明文，只写“见密码管理器”

## 当前状态

- 准备状态：in_review
- 已确认事项：阿里云 ECS 基础规格、公网 IP、操作系统、带宽、到期时间、SSH 登录方式、安全组入方向规则、安全组出方向页面当前为空、阿里云云盘快照已存在、数据库已采购并运行在阿里云 RDS MySQL 8.0
- 阻塞事项：Docker、数据库名称与账号、是否需要开通数据库外网地址、跨地域备份与数据库级备份策略、苹果开发者账号仍待确认
- 备注：当前信息来自控制台截图与 `2026-07-22` 的服务器实机配置；已确认 `80/443` 对公网开放、`22` 仅对指定来源网段开放，安全组 `出方向` 页面当前显示“暂无数据”。`api.gzjiangjiuguan.com` 已启用 HTTPS，HTTP 会跳转到 HTTPS，Certbot 自动续期已启用。当前已见系统盘和数据盘自动云盘快照，数据库已使用独立阿里云 RDS MySQL 8.0 实例，且已确认内网连接地址与 `3306` 端口、外网地址未开通；后续正式部署前仍需确认 Docker、应用监听端口、该安全组在当前账号下的默认出方向策略，以及数据库账号、逻辑备份和跨地域备份方案。
