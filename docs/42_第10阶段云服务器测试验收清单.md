# 第 10 阶段云服务器测试验收清单

更新日期：2026-07-11

本清单区分“已完成的本地验收”和“尚未执行的云端/真实 MySQL 验收”。未获得测试库确认、备份和连接信息前，不执行任何写入型命令。

## 0. 当前结论

| 范围 | 状态 | 证据 |
| --- | --- | --- |
| Prisma generate | 通过 | Prisma Client `6.19.3` 生成成功 |
| Prisma validate | 通过 | `schema.prisma is valid` |
| 静态迁移演练 | 通过 | 从 empty 到 datamodel 生成 44,820 字节 SQL，关键表/字段存在 |
| 真实 MySQL `migrate deploy` | **未执行** | 本机无 `DATABASE_URL`/`.env`/MySQL 客户端，`127.0.0.1:3306` 不可连接 |
| 真实 MySQL seed/幂等重跑 | **未执行** | 同上，不对未确认数据库写入 |
| 后端全量测试 | 通过 | 304/304，fail 0，退出码 0 |
| Flutter analyze | 通过 | No issues found |
| Flutter 全量测试 | 通过 | 131/131，退出码 0 |
| 云端部署/API smoke | **未执行** | 本轮未连接云服务器 |

因此：本地代码和自动化验收通过；第 10 阶段云端验收仍为“待执行”。

## 1. 云端验收信息

| 项目 | 记录 |
| --- | --- |
| 云服务器/环境 | 待填写 |
| API 地址 | 待填写 |
| 数据库实例/库名 | 待填写，必须明确为隔离测试库 |
| 发布版本/提交 | 待填写 |
| 迁移版本 | `20260711000100_stage10_product_cost_schema` |
| 备份/快照 ID | 待填写 |
| 验收人 | 待填写 |
| 验收时间 | 待填写 |

## 2. 写入前安全门槛

- [ ] 通过非敏感方式记录当前 host 和库名，且验收人确认为测试库。
- [ ] 已创建可恢复备份/快照，已记录时间和 ID。
- [ ] 已审阅第 10 阶段 migration：不删表、不重写旧订单成本、不复制三类成本。
- [ ] 已确认回滚策略是“恢复备份/发布修复 migration”，不使用临时 SQL 修改生产数据。
- [ ] 未确认以上四项时，只允许执行代码检查和只读 `migrate status`。

## 3. 可复现的本地验收命令

在 `server/api` 执行：

```powershell
$env:DATABASE_URL='mysql://user:password@host:3306/stage10_test'
npm run prisma:generate
npx prisma validate --schema prisma/schema.prisma
npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script
npm test
```

在 `apps/mobile_desktop` 执行：

```powershell
flutter analyze --no-pub
flutter test
```

本轮后端全量测试第一次在 304 秒工具超时处中断，不计入通过证据；随后在 15 分钟上限下重新完整执行，得到 304/304 通过和退出码 0。

## 4. 云端迁移和 seed 演练

以下命令仅在第 2 节安全门槛全部通过后执行：

```bash
cd /opt/jiangjiu/server/api
npm ci
npm run prisma:generate
npx prisma validate --schema prisma/schema.prisma
npx prisma migrate status
npm run prisma:migrate:deploy
npx prisma migrate status
npm run seed:stage10-products
npm run seed:stage10-products
```

必须保存两次 seed 的 matched/skipped/warnings 输出和数据库统计。

### 4.1 Schema 核对

- [ ] `products` 存在且无排序/库存/批次字段。
- [ ] `product_actual_costs` 存在，成本为整数分，有生效区间和防重叠 trigger。
- [ ] `sales_order_items` 新增 `product_id/unit/actual_unit_cost_cents/actual_cost_subtotal_cents/gross_profit_cents`。
- [ ] 品酒明细和两类扣减规则新增可空 `product_id`。
- [ ] 四个历史关联外键为 `ON DELETE SET NULL`。
- [ ] 旧订单新成本字段仍为 `NULL`，没有用当前成本回填。

### 4.2 16 项数据核对

- [ ] 空库基线下首次导入 16 个商品、16 条初始实际成本。
- [ ] 名称和单位与 `商品实际成本.xlsx` 一致，“小酱酒”为 650 分。
- [ ] 初始生效日为 `2026-07-11`。
- [ ] 第二次导入不新增重复商品或成本。
- [ ] 回填统计包含 matched/skipped/warnings，歧义数据未被猜测。
- [ ] 旧订单名称、单位、成交价、提成、积分和三个成本/毛利字段未被改写。

## 5. API 与权限 smoke

使用独立 admin、finance、boss、sales、front_desk、warehouse、after_sales、taster 测试账号，不在文档中保存 token。

### 5.1 商品与成本

- [ ] admin/finance 可列表、新增、编辑、启停商品。
- [ ] 空名称、空单位、超长值和规范化重名均被稳定拒绝。
- [ ] `DELETE /api/products/:id` 返回 404/路由不存在，商品记录仍可查询。
- [ ] 同一商品启用成本区间重叠返回 `PRODUCT_ACTUAL_COST_RANGE_OVERLAP`。
- [ ] boss 及其他非 admin/finance 角色调用商品成本/毛利接口返回 403。
- [ ] options 只包含启用商品的 `id/name/unit`，无成本、成本历史和审计字段。

### 5.2 订单、品酒和规则

- [ ] 订单和品酒只传 `productName` 被拒绝；只能使用启用 `productId`。
- [ ] 服务端忽略/拒绝客户端伪造名称、单位和成本。
- [ ] 订单按订单日期写入实际成本、成本小计和毛利快照。
- [ ] 修改当前成本后，旧订单快照不变；新订单使用新生效成本。
- [ ] 品酒明细不包含成本和毛利。
- [ ] 销售扣单只按销售扣单规则计算，旅行社扣酒只按旅行社 + 商品规则计算。
- [ ] 修改商品实际成本不改变提成、积分或旅行社扣酒结果。

### 5.3 报表和退款

- [ ] 完整成本返回 `complete` 及精确毛利。
- [ ] 部分成本返回 `partial`，精确毛利为 `null`，包含缺口数和缺口金额。
- [ ] 无成本返回 `unavailable`，不读当前商品成本伪造毛利。
- [ ] 退款订单不自动冲回成本，只返回估算毛利和 `REFUND_COST_REVERSAL_UNAVAILABLE` warning。
- [ ] 成本报表仅 admin/finance 可访问。

## 6. Flutter 云端联调

- [ ] admin/finance 菜单包含“商品管理”，其他角色无该菜单。
- [ ] 手工构造路由时，非 admin/finance 页面内仍拦截且不请求成本。
- [ ] 商品列表、搜索、状态筛选、新增、编辑、启停和成本历史可用。
- [ ] 元金额超过两位小数或格式不完整时无法提交。
- [ ] 订单、品酒、销售扣单、旅行社扣酒字段为可搜索商品选择器，显示“名称 · 单位”。
- [ ] 普通业务角色能调用 options，但 UI 不显示成本字段。
- [ ] 加载失败、空商品列表、停用历史商品和缺 `productId` 快照显示明确状态。

## 7. 审计和数据安全

- [ ] 商品创建、编辑、启停和实际成本变更有 operation log，包含操作者、IP、before/after。
- [ ] 审计日志不含密钥、数据库密码或 token。
- [ ] 无权角色不能通过日志、导出、AI 或错误响应间接获得成本。

## 8. 通过标准

云端验收只能在以下条件全部满足后标记通过：

1. 隔离测试库 migration 和两次 seed 实际成功。
2. 16 项商品/成本准确，回填无猜测，旧成本字段仍为 `NULL`。
3. 权限、自由商品名拒绝、停用、快照不追溯、覆盖状态和退款 warning 的云端 smoke 全部通过。
4. Flutter 连接云端测试 API 完成 admin/finance 与普通角色联调。
5. 已保存版本、验收人、时间、备份 ID、命令输出和失败项记录。

当前上述云端条件尚未执行，所以本文档当前结论为：**本地验收通过，云端验收待执行**。

## 9. 茅台逐瓶库存云端部署与验收

### 9.1 部署步骤

- [ ] 记录当前应用版本、数据库备份 ID、模板哈希和回滚负责人。
- [ ] 部署代码后在 `server/api` 执行 `npm ci`。
- [ ] 对隔离测试库先执行 `npx prisma migrate deploy`，确认迁移 `20260723000200_moutai_serialized_inventory` 成功。
- [ ] 执行 `npm run prisma:generate`、`npm run build`。
- [ ] 确认 `dist/assets/templates/moutai-logistics-sheet.docx` 存在，SHA-256 为 `fff3188e073183db78bb70859718900a1662036e1ca29d89742f9009992ee5aa`。
- [ ] 重启 API，检查健康接口和启动日志；本功能不新增环境变量。
- [ ] 验证统一“茅台”商品的 `inventoryTrackingMode=SERIALIZED`，普通商品仍为 `NONE`。

### 9.2 权限和入库

- [ ] admin/finance/warehouse 菜单显示“茅台”，sales 只在订单中使用选码弹窗。
- [ ] warehouse 可录入名称、日期、批次、序号和物流码，不能看到或提交进货价，结果为 `PENDING_COST`。
- [ ] finance 能补齐进货价并转为 `AVAILABLE`；admin 能管理全部资料。
- [ ] 前导零物流码、生产批次和批次序号入库、查询、导出后完全保留。
- [ ] 同一请求重复物流码和全库重复规范化物流码均被拒绝。
- [ ] 已分配库存普通编辑被拒绝；填写纠错原因后才可修改，并有 before/after operation log。
- [ ] warehouse 的库存 DTO、详情、日志和导出结果均不包含 `purchaseCostCents`。

### 9.3 订单与并发

- [ ] `PENDING_COST`、`ALLOCATED`、`VOID` 和资料不完整记录不出现在可售列表。
- [ ] 可售弹窗只显示名称、物流码、出厂日期、生产批次和批次序号，不显示成本。
- [ ] 同一明细混选不同 `moutaiName` 被拒绝；选择数量自动成为只读订单数量。
- [ ] 两个并发请求抢同一物流码时仅一个成功，另一个返回 `INVENTORY_UNIT_UNAVAILABLE`。
- [ ] 多瓶不同成本的 `actualCostSubtotalCents` 等于逐瓶快照之和，毛利计算准确。
- [ ] 编辑 retained、added、removed 正确；只改客户、售价或备注不改变物流码和成本快照。
- [ ] 历史无物流码订单、普通商品订单、售后和原权限流程正常。
- [ ] 公开二维码销售单不包含库存 ID、物流码内部资料、进货价或内部库存状态。

### 9.4 Word 导出

- [ ] 无选择、超过 100 瓶、重复 ID、不存在、资料不完整和 `VOID` 均返回明确错误。
- [ ] admin、finance、warehouse 可导出；其他角色返回 403。
- [ ] Content-Type、UTF-8 Content-Disposition 文件名和请求顺序正确。
- [ ] 单瓶样例为一页；两瓶合并样例为两页且无额外空白页。
- [ ] 使用 Word 或 LibreOffice 逐页检查 A4 纵向、页边距、字体、18pt 字号、缩进、制表符和签字区对齐均与原模板一致。
- [ ] 检查较长“2024年甲辰龙年生肖茅台酒”正常换行，无缩小字体、截断、重叠或异常分页。
- [ ] 检查带前导零的生产批次、批次序号和物流码保持原样。
- [ ] 注意事项、香型、酒精含量、规格、数量和配料未改写；收货人和收货日期保持空白。
- [ ] 文档不含进货价、数据库 ID 或内部状态；导出日志包含操作者、模板版本和 unitIds。
- [ ] 导出后重新计算原附件和部署模板 SHA-256，确认原附件字节级未变化。

### 9.5 Flutter 联调

- [ ] “单品库存”菜单和标题已显示为“茅台”。
- [ ] 列表筛选、成本列角色可见性、多选/取消、已选数量和导出按钮正确。
- [ ] 无选择时不发请求；导出期间禁止重复点击并显示进度。
- [ ] 成功后用服务端文件名进入 `FilePicker.saveFile`，文件可从用户选择位置正常打开。
- [ ] 保存失败、权限不足、资料不完整和模板损坏均显示中文提示。

### 9.6 通过标准

只有隔离库迁移、角色数据最小化、并发占用、成本快照、公开页防泄露、DOCX 结构测试和 Word/LibreOffice 视觉验收全部通过，才可将本扩展标记为云端验收通过。
