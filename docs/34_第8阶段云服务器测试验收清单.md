# 第 8 阶段云服务器测试验收清单

验收记录日期：2026-07-04

本文用于记录第 8 阶段“数据分析和品鉴师排名”在云服务器测试环境的验收范围、只读 smoke 结果、阻断项、后续补测清单和阶段结论。本文只面向测试环境，不作为生产上线验收记录。

第 8 阶段统计页面只读，不修改订单、旅行团、售后、提成或积分源数据。所有统计指标必须能追溯到原始旅行团、订单或售后记录。任何云端 `migrate`、`seed`、登录、导出、全局开关修改或写入型 smoke，都必须先确认远端数据库是测试库。

AI 自然语言问数、AI 解释和 AI 经营建议属于第 9 阶段，不属于第 8 阶段完成范围。

## 1. 测试 API、数据库和测试数据

### 1.1 测试 API 只读探测结果

| 项目 | 当前结果 | 说明 |
| --- | --- | --- |
| 测试 API 地址 | `http://47.120.24.23` | 本次第 8 阶段云端 smoke 使用的公网入口。 |
| HTTP 端口 | 通过 | `47.120.24.23:80` 可连通。 |
| Nginx 公网响应 | 通过 | 响应头显示 `nginx/1.24.0 (Ubuntu)`。 |
| SSH 密钥文件 | 通过 | 文档记录的 `C:\Users\Msi\.ssh\id_ed25519` 存在。 |
| SSH 端口 | 阻断 | `47.120.24.23:22` TCP 只读探测 5 秒超时，无法进入服务器。 |
| 数据库端口探测 | 阻断 | `47.120.24.23:3306` TCP 只读探测 5 秒超时，未直接连接数据库。 |
| API 鉴权探测 | 通过 | `GET /api/auth/me` 返回 `401`，说明 API 有响应且未登录受保护。 |
| 第 7 阶段接口前置条件 | 通过 | `/api/commission-rules`、`/api/sales-deduction-rules`、`/api/agency-deduction-rules`、`/api/agency-rebate-rules`、`/api/commission-records`、`/api/travel-group-finance-summaries` 均返回 `401`，不再返回 `404`。 |
| 第 8 阶段 overview 路由 | 阻断 | `GET /api/analytics/overview` 返回 `404`。 |
| 第 8 阶段品鉴师排名路由 | 阻断 | `GET /api/analytics/taster-rankings` 返回 `404`。 |
| 第 8 阶段趋势路由 | 阻断 | `GET /api/analytics/trends?granularity=day&metric=net_sales` 返回 `404`。 |
| 第 8 阶段追溯明细路由 | 阻断 | `GET /api/analytics/source/orders` 返回 `404`。 |
| 第 8 阶段导出路由 | 阻断 | `/api/analytics/overview/export` 和 `/api/analytics/taster-rankings/export` 均返回 `404`。 |

结论：第 7 阶段云端接口已不再返回 `404`，但第 8 阶段 analytics 路由在公网 API 上仍返回 `404`。当前远端部署版本很可能未包含第 8 阶段代码，或 `AnalyticsModule` 未正确挂载。

### 1.2 测试数据库确认结果

| 项目 | 当前结果 | 说明 |
| --- | --- | --- |
| 远端数据库是否为测试库 | 未确认 | SSH 不通，无法读取脱敏后的 `DATABASE_URL` 主机、端口和库名。 |
| PM2 状态 | 未执行 | SSH 不通，无法查看进程、环境变量和服务日志。 |
| Nginx 配置状态 | 未执行 | SSH 不通，无法执行 `nginx -t` 或查看站点配置。 |
| 远端部署版本 | 未确认 | SSH 不通，且第 8 阶段路由返回 `404`。 |
| `prisma migrate status` | 未执行 | 测试库未确认前不执行连接数据库的迁移检查。 |
| `prisma migrate deploy` | 未执行 | 测试库未确认前不执行写入型迁移。 |
| seed | 未执行 | 测试库未确认前不写入测试数据。 |
| 登录和写入型 smoke | 未执行 | 登录、导出、全局标记过滤切换和操作日志查询都可能写库或依赖测试库确认。 |

安全结论：本次未连接生产库，未覆盖生产数据，未执行云端 migrate，未执行 seed，未登录，未写入 smoke 数据。

### 1.3 测试数据命名规则

- 所有第 8 阶段云端 smoke 的客户、订单、旅行团、售后、备注、导出文件名和操作说明必须包含 `test` 或 `smoke`。
- 不使用真实客户、真实手机号、真实地址、真实订单号或真实售后内容。
- seed 必须幂等，可重复执行，不删除旧测试数据。
- 第 8 阶段样例至少覆盖：今日、本月、上月或可测日期；至少 2 个品鉴师；有有效订单团；无有效订单团；部分退款且 `financeConfirmed=true`；未确认退款；已标记和未标记客户/旅行团。
- 全局标记过滤 smoke 如需修改开关，执行前记录原值，结束前必须由管理员恢复原状态。
- 不在日志、文档、截图或命令输出中记录真实密码、token、完整数据库连接串或密钥。

## 2. 角色验收项

### 2.1 管理员 admin

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| ADMIN-01 | 管理员登录 | 可登录测试 API，并看到数据分析入口 | 未执行，测试库未确认且 analytics 路由 `404` |
| ADMIN-02 | 经营看板 | 可查看 overview 指标、warnings 和追溯入口 | 阻断，`/api/analytics/overview` 返回 `404` |
| ADMIN-03 | 品鉴师排名 | 可查看排名、排序并进入详情 | 阻断，`/api/analytics/taster-rankings` 返回 `404` |
| ADMIN-04 | 趋势图 | 可查看销售额、接待团数、打蛋率趋势 | 阻断，trends 路由 `404` |
| ADMIN-05 | 追溯明细 | 可查看订单、旅行团、售后退款明细 | 阻断，source 路由 `404` |
| ADMIN-06 | 导出 | 可导出经营看板和品鉴师排名 | 阻断，export 路由 `404` |
| ADMIN-07 | 全局标记过滤恢复 | smoke 结束前可恢复原开关状态 | 未执行，未修改全局开关 |
| ADMIN-08 | 操作日志 | 可查看导出日志，action 完整 | 未执行，导出未执行 |

### 2.2 老板 boss

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| BOSS-01 | 老板登录 | 可登录并看到数据分析入口 | 未执行，测试库未确认 |
| BOSS-02 | 经营看板只读 | 可查看指标，不修改源数据 | 阻断，overview 路由 `404` |
| BOSS-03 | 日期预设和自定义日期 | 可切换本年、本月、上个月、近 10 天、昨日、今日和自定义范围 | 云端未执行 |
| BOSS-04 | 品鉴师排名详情 | 可查看排名并进入品鉴师详情 | 阻断，ranking 路由 `404` |
| BOSS-05 | 趋势和追溯 | 可查看趋势图和来源明细 | 阻断，trends/source 路由 `404` |
| BOSS-06 | 导出 | 可按权限导出经营看板和品鉴师排名 | 阻断，export 路由 `404` |
| BOSS-07 | 全局标记过滤 | 开启后不展示未标记客户或未标记旅行团关联数据 | 云端未执行 |

### 2.3 财务 finance

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| FIN-01 | 财务登录 | 可登录并看到数据分析入口 | 未执行，测试库未确认 |
| FIN-02 | 统计查看 | 可查看 overview、ranking、trends、details | 阻断，analytics 路由 `404` |
| FIN-03 | 导出 | 可导出经营看板和品鉴师排名 | 阻断，export 路由 `404` |
| FIN-04 | 标记过滤 | 统计和导出遵守全局标记过滤 | 云端未执行 |
| FIN-05 | 售后退款口径 | 已确认退款扣减，未确认退款仅进入 warning | 云端未执行 |

### 2.4 品鉴师 taster

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| TASTER-01 | 数据分析入口 | 默认不显示公司级数据分析入口 | 未执行，未登录 |
| TASTER-02 | 越权访问 | 直接访问公司级 analytics API 应返回 `403` 或不可见 | 未执行，当前路由先返回 `404`，无法验证权限 |
| TASTER-03 | 既有功能 | 保留本人旅行团、总结和提成相关入口 | 云端未执行 |

### 2.5 销售 sales

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| SALES-01 | 数据分析入口 | 默认不显示公司级数据分析入口 | 未执行，未登录 |
| SALES-02 | 越权访问 | 直接访问公司级 analytics API 应返回 `403` 或不可见 | 未执行，当前路由先返回 `404` |
| SALES-03 | 既有订单功能 | 保留订单查询、开单和二维码销售单相关能力 | 云端未执行 |

### 2.6 售后 after_sales

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| AS-01 | 数据分析入口 | 默认不显示公司级数据分析入口 | 未执行，未登录 |
| AS-02 | 越权访问 | 直接访问公司级 analytics API 应返回 `403` 或不可见 | 未执行，当前路由先返回 `404` |
| AS-03 | 既有售后功能 | 保留售后创建和状态流转能力 | 云端未执行 |

### 2.7 库管 warehouse

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| WH-01 | 数据分析入口 | 默认不显示公司级数据分析入口 | 未执行，未登录 |
| WH-02 | 越权访问 | 直接访问公司级 analytics API 应返回 `403` 或不可见 | 未执行，当前路由先返回 `404` |
| WH-03 | 既有库管功能 | 保留发货、物流和打包相关能力 | 云端未执行 |

### 2.8 前台 front_desk

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| FRONT-01 | 数据分析入口 | 默认不显示公司级数据分析入口 | 未执行，未登录 |
| FRONT-02 | 越权访问 | 直接访问公司级 analytics API 应返回 `403` 或不可见 | 未执行，当前路由先返回 `404` |
| FRONT-03 | 全局标记过滤开关 | 如前台可开启全局过滤，结束后必须由管理员恢复 | 云端未执行，未修改开关 |
| FRONT-04 | 既有旅行团功能 | 保留旅行团录入和查询能力 | 云端未执行 |

## 3. 专项验收

### 3.1 日期预设

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| DATE-01 | 今日 `today` | 按 Asia/Shanghai 计算今日，边界包含 dateFrom/dateTo | 本地测试已覆盖；云端未执行 |
| DATE-02 | 昨日 `yesterday` | 返回昨日单日范围 | 本地测试已覆盖；云端未执行 |
| DATE-03 | 近 10 天 `last_10_days` | 包含今天在内的近 10 天 | 本地测试已覆盖；云端未执行 |
| DATE-04 | 本月 `this_month` | 本月 1 日到今天 | 本地测试已覆盖；云端未执行 |
| DATE-05 | 上个月 `last_month` | 上月 1 日到上月最后一天 | 本地测试已覆盖；云端未执行 |
| DATE-06 | 本年 `this_year` | 本年 1 月 1 日到今天 | 本地测试已覆盖；云端未执行 |
| DATE-07 | 自定义 `custom` | 校验非法日期和 dateFrom/dateTo 顺序 | 本地测试已覆盖；云端未执行 |

### 3.2 经营看板 overview

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| OVERVIEW-01 | 销售金额 | 返回 `grossSalesAmountCents`、`refundAmountCents`、`pendingRefundAmountCents`、`netSalesAmountCents` | 本地 API 测试已覆盖；云端路由 `404` |
| OVERVIEW-02 | 接待指标 | 返回 `totalGroupCount`、`totalGuestCount`、团均、人均 | 本地 API 测试已覆盖；云端未执行 |
| OVERVIEW-03 | 打蛋率 | 返回 `noEffectiveOrderGroupCount`、`conversionGroupCount`、`noOrderRate`、`conversionRate` | 本地 API 测试已覆盖；云端未执行 |
| OVERVIEW-04 | warnings | 返回未确认退款、退款状态不一致、缺失品鉴师等 warning | 本地测试已覆盖；云端未执行 |
| OVERVIEW-05 | 只读 | 普通查询不写 `operation_logs`，不修改源数据 | 本地测试已覆盖；云端未执行 |

### 3.3 品鉴师排名

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| RANK-01 | 默认排序 | 默认按 `netSalesAmountCents` 降序 | 本地测试已覆盖；云端路由 `404` |
| RANK-02 | 排序项 | 支持销售额、接待团数、接待人数、团均、人均、打蛋率排序 | 本地测试已覆盖；云端未执行 |
| RANK-03 | 未分配品鉴师 | `tasterId` 为空归入未分配品鉴师或返回 warning | 本地测试已覆盖；云端未执行 |
| RANK-04 | 详情 | 可查看该品鉴师摘要、旅行团、订单和售后退款明细 | 本地测试已覆盖；云端未执行 |
| RANK-05 | 全局标记过滤 | 未标记旅行团不进入排名 | 本地测试已覆盖；云端未执行 |

### 3.4 打蛋率

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| NOORDER-01 | 无有效订单旅行团 | 无有效订单团计入 `noEffectiveOrderGroupCount` | 本地测试已覆盖；云端未执行 |
| NOORDER-02 | 有有效订单旅行团 | 有 `valid` 或 `partial_refund` 且有效金额大于 0 的订单团计入转化 | 本地测试已覆盖；云端未执行 |
| NOORDER-03 | 分母为 0 | 分母为 0 时比例返回 0 | 本地测试已覆盖；云端未执行 |
| NOORDER-04 | 明细追溯 | 可通过 source/travel-groups 追溯 noEffectiveOrder=true 的旅行团 | 本地测试已覆盖；云端未执行 |

### 3.5 趋势图

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| TREND-01 | 按日趋势 | `granularity=day` 返回稳定日粒度趋势点 | 本地测试已覆盖；云端路由 `404` |
| TREND-02 | 按月趋势 | `granularity=month` 返回稳定月粒度趋势点 | 本地测试已覆盖；云端未执行 |
| TREND-03 | 销售金额趋势 | 按 `sales_orders.orderDate` 统计 | 本地测试已覆盖；云端未执行 |
| TREND-04 | 接待和打蛋率趋势 | 按 `travel_groups.visitDate` 统计 | 本地测试已覆盖；云端未执行 |
| TREND-05 | 空数据 | 无数据时返回空数组或 0 值点，前端展示稳定 | 本地测试已覆盖；云端未执行 |

### 3.6 追溯明细

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| SOURCE-01 | 订单明细 | `/api/analytics/source/orders` 支持 sales、refund、group_scoped、taster 等来源筛选 | 本地测试已覆盖；云端路由 `404` |
| SOURCE-02 | 旅行团明细 | `/api/analytics/source/travel-groups` 支持 `noEffectiveOrder=true` | 本地测试已覆盖；云端未执行 |
| SOURCE-03 | 售后明细 | `/api/analytics/source/after-sales` 只返回通过订单作用域的售后记录 | 本地测试已覆盖；云端未执行 |
| SOURCE-04 | 敏感信息 | 不返回密码、token、完整数据库连接串等敏感信息 | 本地测试已覆盖；云端未执行 |
| SOURCE-05 | 标记过滤 | 明细不泄露未标记客户或未标记旅行团关联数据 | 本地测试已覆盖；云端未执行 |

### 3.7 导出

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| EXPORT-01 | 经营看板导出 | 导出包含指标摘要、订单明细、旅行团明细、售后退款明细 | 本地测试已覆盖；云端 export 路由 `404` |
| EXPORT-02 | 品鉴师排名导出 | 导出包含排名摘要和品鉴师明细 | 本地测试已覆盖；云端 export 路由 `404` |
| EXPORT-03 | 筛选一致 | 导出筛选条件与页面 API 一致 | 本地测试已覆盖；云端未执行 |
| EXPORT-04 | 权限 | admin、boss、finance 可导出，其他角色不可导出 | 本地测试已覆盖；云端未执行 |
| EXPORT-05 | 敏感信息 | 不导出密码、token、完整数据库连接串等敏感信息 | 本地测试已覆盖；云端未执行 |

### 3.8 全局标记过滤

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| MARK-01 | overview 订单过滤 | 开启后不统计未标记客户或未标记旅行团关联订单 | 本地测试已覆盖；云端未执行 |
| MARK-02 | ranking 旅行团过滤 | 开启后未标记旅行团不进入排名 | 本地测试已覆盖；云端未执行 |
| MARK-03 | details/source 明细过滤 | 明细不泄露未标记数据 | 本地测试已覆盖；云端未执行 |
| MARK-04 | trends 过滤 | 趋势不统计未标记数据 | 本地测试已覆盖；云端未执行 |
| MARK-05 | export 过滤 | 导出与页面 API 使用同一过滤口径 | 本地测试已覆盖；云端未执行 |
| MARK-06 | 开关恢复 | 云端 smoke 结束前由管理员恢复原状态 | 未执行，未修改开关 |

### 3.9 操作日志

| 编号 | 验收项 | 期望结果 | 当前状态 |
| --- | --- | --- | --- |
| LOG-01 | 普通读取不写日志 | overview、ranking、trend 读取不写 `operation_logs` | 本地测试已覆盖；云端未执行 |
| LOG-02 | 经营看板导出日志 | 导出写 `analytics.overview.export` | 本地测试已覆盖；云端未执行 |
| LOG-03 | 品鉴师排名导出日志 | 导出写 `analytics.taster_rankings.export` | 本地测试已覆盖；云端未执行 |
| LOG-04 | 日志字段 | 包含 `entityType`、`entityId`、`beforeData=null`、筛选摘要、导出行数、`userId`、`ipAddress` | 本地测试已覆盖；云端未执行 |
| LOG-05 | 日志安全 | 不记录真实密码、token、完整数据库连接串 | 本地测试已覆盖；云端未执行 |

## 4. 本阶段已完成、未完成和风险

### 4.1 已完成

- 已形成第 8 阶段开发文档：`docs/31_第8阶段数据分析和品鉴师排名开发文档.md`。
- 已形成第 8 阶段逐次开发提示词：`docs/32_第8阶段逐次开发提示词.md`。
- 已形成第 8 阶段 Schema 差距清单：`docs/33_第8阶段数据分析和品鉴师排名Schema差距清单.md`。
- 本地工作区已包含第 8 阶段组合索引 migration、analytics 后端模块、helper、API、导出日志、seed 样例和后端测试相关改动。
- 本地工作区已包含 Flutter 第 8 阶段 API 模型、BusinessApi 方法、数据分析页、首页轻量指标、角色菜单权限和 Flutter 测试相关改动。
- 本地后端验证已完成：`npm.cmd run prisma:generate`、本地 migration、`npm.cmd run prisma:seed`、`npm.cmd test` 通过；本地 in-process API smoke 通过。
- Flutter 本地验证已完成：`dart analyze`、`flutter test`、Windows debug 构建通过。
- 本次云端只读 smoke 已确认第 7 阶段接口不再返回 `404`，均受鉴权保护返回 `401`。
- 本次云端只读 smoke 已确认第 8 阶段 analytics 路由仍返回 `404`，因此及时停止所有写入型验收。
- 本次未连接生产库，未覆盖生产数据，未执行云端 migrate，未执行 seed，未登录，未写入 smoke 数据。

### 4.2 未完成

- 未确认远端数据库是测试库。
- 未确认远端 PM2、Nginx 配置、后端部署目录、部署 commit 或部署版本。
- 未执行 `prisma migrate status`。
- 未执行第 8 阶段组合索引 migration 的云端 `prisma migrate deploy`。
- 未执行云端 seed。
- 未执行老板、管理员、财务和无权限角色的云端登录 smoke。
- 未在云端验证六个日期预设和自定义日期。
- 未在云端核对 overview 指标、品鉴师排名、打蛋率、趋势图和追溯明细。
- 未在云端执行经营看板导出和品鉴师排名导出。
- 未在云端验证全局标记过滤开启和恢复。
- 未查看云端 `operation_logs`，无法确认导出日志完整性。

### 4.3 风险

- 第 8 阶段 analytics 路由全部返回 `404`，当前远端部署版本很可能未包含第 8 阶段代码，或模块未挂载。
- SSH 端口不通，无法进行服务器内只读检查，也无法确认测试库库名。
- 远端数据库未确认是测试库前，任何 migrate、seed、登录、导出和全局开关修改都有误操作生产数据风险。
- 全局标记过滤是全局开关，未来云端 smoke 必须先记录原值，结束前恢复，避免影响其他测试人员。
- 第 8 阶段统计依赖订单、旅行团、售后退款事实和第 7 阶段云端部署稳定性；虽然第 7 阶段路由已不再 `404`，但第 8 阶段云端尚未完成闭环。
- 本地工作区存在大量未提交改动和未跟踪文件，远端部署版本可能明显落后于本地工作区。

### 4.4 是否可进入第 9 阶段

当前结论：不建议以第 8 阶段云端验收完成为依据进入第 9 阶段正式开发。

可以进入第 9 阶段的只读设计讨论或文档预研，但正式推进 AI 自然语言问数、AI 解释和 AI 经营建议前，应先完成以下事项：

1. 恢复或确认 SSH、堡垒机、云控制台命令等只读运维入口。
2. 只读确认远端 `DATABASE_URL` 脱敏后的主机、端口和库名，确认是测试库。
3. 确认远端部署版本包含第 8 阶段代码，并让 `/api/analytics/*` 从 `404` 变为受鉴权保护的 `401/403` 或正常响应。
4. 在测试库确认后执行 `prisma migrate status`。
5. 确认第 8 阶段组合索引 migration 只包含索引相关 DDL 后，再执行 `prisma migrate deploy`。
6. 执行 seed，并确认 smoke/test 命名的统计样例数据存在。
7. 按本文第 2、3 节补跑管理员、老板、财务、无权限角色、日期预设、overview、ranking、打蛋率、trends、source、export、全局标记过滤和 operation_logs 云端 smoke。
8. 验收结束前恢复全局标记过滤原状态，并保存新的验收记录。

### 4.5 第 9 阶段云端 smoke 前复核（2026-07-05）

本次复核仅基于本文既有云端验收记录、当前本地文档和本地代码状态进行确认；未执行云端登录、`migrate`、`seed`、导出、全局开关修改、AI 对话写入或任何第 9 阶段云端写入型 smoke。

| 必须确认项 | 当前复核结果 | 结论 |
| --- | --- | --- |
| 1. 远端数据库是测试库 | 仍未确认。本文已记录 SSH 不通，无法读取脱敏后的 `DATABASE_URL` 主机、端口和库名。 | 未闭环 |
| 2. 远端部署版本包含第 8 阶段 analytics 代码 | 仍未确认。本文记录第 8 阶段 analytics 路由返回 `404`，说明远端版本很可能未包含第 8 阶段代码或模块未挂载。 | 未闭环 |
| 3. 第 8 阶段 analytics 路由不再返回 `404` | 不满足。`/api/analytics/overview`、`/api/analytics/taster-rankings`、`/api/analytics/trends`、`/api/analytics/source/orders`、analytics export 路由在既有记录中均为 `404`。 | 未闭环 |
| 4. `prisma migrate status` 正常 | 未执行。测试库未确认前，不执行任何连接远端数据库的迁移检查。 | 未闭环 |
| 5. 如需 `migrate deploy`，已确认只作用于测试库 | 未确认。不具备执行条件；测试库未确认前不得执行云端 `migrate deploy`。 | 未闭环 |
| 6. seed 已准备 smoke/test 命名数据 | 本地 seed 已准备第 8 阶段 `stage8 smoke/test` 命名样例、已标记/未标记数据、退款和品鉴师排名样例；云端 seed 未执行。 | 本地准备完成，云端未闭环 |
| 7. 老板 overview、ranking、trends、source、export 可用 | 未验证成功。analytics 路由仍是 `404`，且未登录老板账号做云端 smoke。 | 未闭环 |
| 8. 财务可访问允许的统计 | 未验证。测试库未确认且 analytics 路由 `404`，未执行财务登录 smoke。 | 未闭环 |
| 9. 无权限角色返回 `403` | 未验证。当前 analytics 路由先返回 `404`，无法证明无权限角色的 `403` 鉴权链路。 | 未闭环 |
| 10. 全局标记过滤开启和恢复已验证 | 未验证。测试库未确认前不修改全局开关；本文记录未执行开启和恢复。 | 未闭环 |
| 11. `operation_logs` 已验证 | 未验证。导出未执行，云端 `operation_logs` 未查看，无法确认导出日志完整性。 | 未闭环 |

复核结论：第 8 阶段云端补验收尚未闭环，不允许进入第 9 阶段云端写入型 smoke。第 9 阶段当前只允许继续做本地开发、本地测试、本地文档，以及不登录、不写库的云端只读探测；在远端测试库、远端部署版本、第 8 阶段 analytics 路由和第 8 阶段云端 smoke 全部确认前，不得执行第 9 阶段云端 `migrate`、`seed`、登录、`POST /api/ai/chat`、AI 对话记录写入、全局开关修改或任何会写入数据库的验证。

## 5. 不属于第 8 阶段完成范围

- AI 自然语言问数。
- AI 指标解释。
- AI 经营建议。
- AI 对话记录、AI 权限控制和 AI 工具调用。
- 第 10 阶段上线运维、监控告警、正式生产发布、备份恢复演练和生产性能优化。
- 生产级统计快照、离线 OLAP、多维 BI 自助报表和任意拖拽维度分析。
